[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "scp-realitybench"
version = "0.1.0.dev0"
description = "Independent falsification and reality-verification benchmark for frontier LLMs"
requires-python = ">=3.11"

[project.scripts]
rb = "realitybench.cli:main"
